# TODO
- [ ] Replace chatbot launcher icon (`.chatbot-float`) with robot markup on all pages that contain it
- [ ] Update chatbot window header to include the same robot avatar on all pages that contain it
- [ ] Add CSS for robot styling + idle/hover/click animations (optimized, no Lottie)
- [ ] Update `js/chatbot.js` to trigger an `is-opening` class on the launcher when opening
- [ ] Verify desktop + mobile behavior on `index.html` and at least one other page

